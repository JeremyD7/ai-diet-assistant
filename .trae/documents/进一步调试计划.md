
# 进一步调试和修复 - 实施计划

## 问题分析

错误信息："AI 返回格式不正确，无法提取 JSON"，但缺少关键的调试日志信息。

## 可能原因

1. **模型问题** - `Qwen/Qwen3.5-122B-A10B` 可能不支持图像输入格式
2. **图像 URL 访问问题** - 图像 URL 可能无法被 AI 访问
3. **纯文本也有问题** - 可能整个 API 调用流程有问题

## 修复方案

### 1. 添加更多调试信息
把 AI 返回的原始内容完整记录下来

### 2. 先测试纯文本功能
暂时注释掉图像输入，先确保纯文本描述能正常工作

### 3. 改进 JSON 提取逻辑
使用更灵活的方式提取 JSON，或者直接返回 AI 内容用于调试

## 修改内容

修改文件：`supabase/functions/analyze-food/index.ts`
